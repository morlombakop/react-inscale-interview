/*
 * AddNewPage Messages
 *
 * This contains all the text for the AddNewPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'inscaleInterview.containers.AddNewPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Add News Page',
  },
});
